{
  "name": "aionos-diagnostics-root",
  "private": true,
  "scripts": {
    "build": "node -e \"if (require('fs').existsSync('frontend')) { require('child_process').execSync('cd frontend && npm install && npm run build', {stdio: 'inherit'}); } else { require('child_process').execSync('npm run build', {stdio: 'inherit'}); }\""
  }
}
